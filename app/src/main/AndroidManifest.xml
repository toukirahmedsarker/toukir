<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.medicino.www.medicino">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!--
 ATTENTION: This was auto-generated to add Google Play services to your project for
     App Indexing.  See https://g.co/AppIndexing/AndroidStudio for more information.
        -->
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <activity android:name=".Patient_Register" />
        <activity android:name=".PharmacyRegistration" />
        <activity android:name=".MedicineRegistration" />
        <activity android:name=".DoctorRegistration" />
        <activity android:name=".PharmaciticalCompanyRegistration" />
        <activity android:name=".Doctors_Patient_Registration" />
        <activity android:name=".Patient_Self_registration" />
        <activity android:name=".LogIN" />
        <activity android:name=".PatientScreen" />
        <activity android:name=".DoctorScreen" />
        <activity android:name=".PharmacyScreen" />
        <activity android:name=".CompanyScreen" />
        <activity android:name=".About" />
        <activity android:name=".DoctorSearch" />
        <activity android:name=".SearchResult"></activity>
    </application>

</manifest>